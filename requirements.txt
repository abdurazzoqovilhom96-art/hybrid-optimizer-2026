# Pinned for reproducibility. Tested on Python 3.11 / 3.12, Windows x64 and Linux.
numpy>=1.26,<3
scipy>=1.11
pandas>=2.0
joblib>=1.3
matplotlib>=3.7

# CEC'2017 and CEC'2022 benchmark suites with their official shift and rotation
# data. Note: opfunu numbers CEC'2017 F1..F29 because it already dropped the
# withdrawn F2; temoa/suites.py maps that onto official ids.
opfunu>=1.0.4

# Nikolaus Hansen's own CMA-ES. Used rather than a reimplementation: a faulty
# reimplementation of a competitor errs in our own favour.
cma>=4.0
